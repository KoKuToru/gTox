namespace flatbuffers.Config;

//never reorder these properties !

table Global {
	connection_udp: bool (deprecated);
	connection_tcp: bool (deprecated);
	
	proxy_type: int (deprecated);
	proxy_host: string (deprecated);
	proxy_port: int (deprecated);
	
	theme_color: int;
	
	profile_remember: bool;
	
	av_video_default: string;
}

root_type Global;
